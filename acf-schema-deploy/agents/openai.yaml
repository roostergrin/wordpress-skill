interface:
  display_name: "ACF Schema Deploy"
  short_description: "Safe ACF schema pull/push workflow via WordPress plugin API"
  default_prompt: "Use $acf-schema-deploy for schema pull, push, bootstrap, and plugin deployment. Follow the neutral workflow in skills/acf-schema-deploy.md and prefer the wp-acf wrapper command surface."
